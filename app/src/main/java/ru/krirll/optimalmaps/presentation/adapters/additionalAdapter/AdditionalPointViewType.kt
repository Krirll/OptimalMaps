package ru.krirll.optimalmaps.presentation.adapters.additionalAdapter

import ru.krirll.optimalmaps.R

enum class AdditionalPointViewType(val layout: Int) {
    ADD_NEW(R.layout.add_new_additional_point),
    POINT_ITEM(R.layout.additional_point_item)
}